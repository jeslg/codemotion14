# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Dictionary Routes

GET    /			controllers.DictionaryApp.helloDictionary

POST   /			controllers.DictionaryApp.add

GET    /:word			controllers.DictionaryApp.search(word)

GET    /socket/add		controllers.DictionaryApp.socketAdd

# Alternative Dictionary Routes

GET    /ws/token		controllers.AlternativeDictionaryApp.wsToken

GET    /ws/:word		controllers.AlternativeDictionaryApp.wsSearch(word)
