# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Dictionary Routes

GET    /			controllers.DictionaryApp.helloDictionary

GET    /:word			controllers.DictionaryApp.furtherSearch(word)

PUT    /:word			controllers.DictionaryApp.add(word)

GET    /ws/add			controllers.DictionaryApp.wsAdd

# Alternative Dictionary Routes

GET    /alt/token		controllers.AlternativeDictionaryApp.token

GET    /alt/:word		controllers.AlternativeDictionaryApp.search(word)
